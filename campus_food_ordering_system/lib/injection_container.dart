// Dependency Injection Container
//
// This file will be used to register and provide dependencies
// throughout the application using a service locator pattern.
// As the app grows, add packages like `get_it` and register
// your repositories, use cases, and blocs/cubits here.

Future<void> init() async {
  // TODO: Register dependencies here
  // Example (with get_it):
  //   final sl = GetIt.instance;
  //   sl.registerLazySingleton(() => SomeRepository());
}
